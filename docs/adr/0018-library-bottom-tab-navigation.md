# ADR-0018: 蔵書を全体・シリーズ・非表示・設定の下部タブに分離する

- Status: Accepted
- Date: 2026-08-10
- Amended: 2026-08-14

## Context

蔵書画面では、Google Play Books の同期、表示中/非表示の切り替え、シリーズ単位の折りたたみ表示、書籍ごとのシリーズ編集が1つの縦長画面に混在していた。

一方、RSS・Reddit・ブックマークでは、同一機能内の主要な表示モードを画面下部のタブで切り替える。蔵書も同じ操作モデルに揃え、閲覧と管理設定を分離したい。

ADR-0017 ではシリーズ単位にまとめる表示を蔵書一覧そのものとして定義していたが、シリーズを意識せず全冊を確認したい場合にはフラットな一覧も必要になる。

3列程度の書籍グリッドでは、各書籍に「シリーズ」「非表示」などの操作ボタンを常設すると横幅に収まりにくく、タイトルや著者情報より操作ラベルが目立つ。これらは日常的な閲覧より頻度の低い管理操作であるため、一覧の情報密度を優先したい。

当初は非表示の蔵書一覧を設定タブの下部に配置していたが、同期操作や表紙補完設定の下に一覧が続くため、端末上で一覧に使える表示領域が小さくなる。非表示書籍は設定値ではなく独立して閲覧・復元するコレクションとして扱う方が、他の蔵書表示と操作モデルを揃えられる。

## Decision

蔵書 feature の画面を次の4タブに分け、Material 3 の下部 NavigationBar で切り替える。

- 全体: 非表示ではない蔵書をシリーズでまとめず、書籍単位のグリッドとして表示する
- シリーズ: ADR-0017 のシリーズ推定・手動設定を利用し、シリーズ見出しを折りたたみ表示する。シリーズ未設定の書籍もこの画面から確認できる
- 非表示: 非表示にした蔵書を全画面の書籍グリッドとして表示し、再表示操作を提供する。全体タブと同じサービス別フィルタを利用できる
- 設定: Google Play Books の同期状態と同期操作、Kindle の表紙補完設定など、蔵書サービスに関する設定だけを提供する

書籍の「シリーズ編集」と「非表示」は、全体・シリーズの書籍カバーを長押ししたときに表示するアクションメニューへまとめる。非表示タブでも同じ長押しメニューを使用し、「シリーズ編集」と「再表示」を提供する。これらの操作ボタンは書籍カード下へ常設しない。

書籍カバーの通常タップは、情報 URL がある書籍では従来通りその URL を開く。情報 URL がない書籍では通常タップでもアクションメニューを表示し、結果のないクリック操作をアクセシビリティに公開しない。長押しによるアクションメニューはすべての書籍で利用できる。

蔵書タブは library feature 内の表示状態として扱い、domain/data のモデルや永続化形式は変更しない。アプリ全体のドロワー上では引き続き「蔵書」を1つの section として扱う。

サービス別フィルタは全体・シリーズ・非表示タブで共有する。タブを移動しても選択中のサービスを維持し、同じサービス由来の蔵書を続けて確認できるようにする。

蔵書画面はアプリ全体の Scaffold 内に配置されるため、library feature 側の NavigationBar では system bar inset を重複適用しない。

## Consequences

### Positive

- RSS 等と同様に、蔵書内の主要な表示を下部タブで直接切り替えられる
- 全冊をフラットに見る用途と、シリーズ単位で見る用途を分離できる
- 非表示書籍に画面全体のグリッド領域を使えるため、多数の書籍を確認・復元しやすい
- 設定タブから書籍一覧を除外でき、同期・表紙補完などの設定に役割を限定できる
- 書籍ごとの管理ボタンを常設しないため、狭い3列グリッドでもタイトル・著者情報のレイアウトを確保しやすい
- 全体・シリーズ・非表示一覧で書籍操作の入口を長押しに統一できる
- 情報 URL がない書籍も通常タップまたは長押しで管理操作へ到達できる
- domain/data の変更が不要で、既存のシリーズ情報・非表示状態・同期キャッシュをそのまま利用できる

### Negative

- 蔵書 feature 内に独立したタブ状態を持つため、アプリ全体の MainTab とは別に画面状態を管理する必要がある
- 下部 NavigationBar が4項目になるため、3項目時より各タブの横幅は小さくなる
- 長押し操作は常設ボタンより発見しにくいため、アクセシビリティ上の長押しラベルを付与して操作意図を公開する必要がある

## Relationship to existing ADRs

- ADR-0013 のサービス非依存な蔵書モデルと Google Play Books 同期方式は変更しない
- ADR-0014 の非表示状態の永続化方式は変更せず、非表示一覧の表示場所と操作入口だけを変更する
- ADR-0017 のシリーズ推定・手動設定・折りたたみ表示を維持し、その表示場所をシリーズタブとして明確化する
- ADR-0017 に記載された「シリーズ表示を蔵書一覧そのものとする」UI 配置については、本 ADR の全体/シリーズ分離を優先する
